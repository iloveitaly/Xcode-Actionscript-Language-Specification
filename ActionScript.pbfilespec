/*
	ActionScript source file specs.
 */

(
    {
      Identifier = sourcecode.actionscript;
      BasedOn = sourcecode;
      Name = "ActionScript Files";
      Extensions = (as, actionscript);
      ComputerLanguage = actionscript;
    }
)